# =============================================================================
# Django Environment Variables - Example Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to Git!
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Fill in your values in the .env file
#   3. Install python-decouple: pip install python-decouple
#   4. The .env file is automatically loaded by settings.py
# =============================================================================

# =============================================================================
# Microsoft 365 OAuth2 Configuration (RECOMMENDED)
# =============================================================================
# Get these values from Azure Portal > App Registrations > Your App
# See EMAIL_SETUP.md for detailed instructions

# Application (client) ID from Azure App Registration
MICROSOFT_CLIENT_ID=your_application_client_id_here

# Client secret value (NOT the secret ID)
# Get from Azure Portal > App Registrations > Your App > Certificates & secrets
MICROSOFT_CLIENT_SECRET=your_client_secret_value_here

# Directory (tenant) ID from Azure App Registration
MICROSOFT_TENANT_ID=your_tenant_id_here

# =============================================================================
# Traditional SMTP Authentication (LEGACY - Optional)
# =============================================================================
# Only needed if you're using traditional SMTP instead of OAuth2
# Requires Microsoft 365 App Password (requires 2FA enabled)
# To use this instead of OAuth2, update EMAIL_BACKEND in settings.py

# EMAIL_PASSWORD=your_microsoft_365_app_password_here

# =============================================================================
# Django Secret Key (Production)
# =============================================================================
# In production, generate a new secret key and set it here
# Generator: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# SECRET_KEY=your_production_secret_key_here

# =============================================================================
# Database Configuration (Production - Optional)
# =============================================================================
# For production, you might want to use PostgreSQL, MySQL, etc.
# Uncomment and configure as needed

# DATABASE_URL=postgresql://user:password@localhost/dbname

# =============================================================================
# Debug Settings (Development)
# =============================================================================
# Set to False in production!

# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1

# =============================================================================
# Email Testing (Development)
# =============================================================================
# Uncomment to use console backend (emails printed to terminal)
# This is useful for testing without sending real emails

# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# =============================================================================
# Other Optional Settings
# =============================================================================

# STATIC_ROOT=/path/to/static
# MEDIA_ROOT=/path/to/media
